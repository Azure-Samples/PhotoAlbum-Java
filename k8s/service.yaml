apiVersion: v1
kind: Service
metadata:
  name: photo-album
  namespace: photo-album
  labels:
    app: photo-album
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: photo-album
